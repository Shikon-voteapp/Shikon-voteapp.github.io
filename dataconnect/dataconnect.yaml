specVersion: "v1beta"
serviceId: "shikonvoteapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "shikonvoteapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]

---

prev: 
    text: ホーム
    link: index
next:
  text: 環境構築
  link: installation
---


# 紫紺祭投票アプリとは？
紫紺祭投票アプリは、明治大学付属明治中学校・明治高等学校の文化祭である「紫紺祭」においての展示投票を行うためのシステムです。Flutterをベースに動作し、Firebaseとの連携機能を用い、Google Cloudのサーバー上に投票データを保存します。
## 機能
紫紺祭投票アプリの最大の特徴は、その汎用性にあります。クロスプラットフォーム開発の代表的プラットフォームであるFlutterを採用しているため、1つのコードをベースにさまざまなプラットフォーム(例：iOS、Android、Web、Windows)でアプリケーションを動作させることができます。

また、コードを直接操作しなくてもデータのアップデートやメンテナンスが行えるよう、ビジュアルベースの設定ユーティリティも充実しています。

## システム体系図
```
┌─────────┐
│  利用者  │
└────┬────┘
     │
     ├───────────────┐
     │               │
┌────▼─────┐   ┌─────▼─────┐
│ ブラウザ  │   │ Windows   │
│  (PWA)   │   │ (Electron) │
└────┬─────┘   └────┬───────┘
     │               │
     └──────┬────────┘
            ▼
      ┌──────────────────────┐
      │ Flutter Web UI       │
      │  lib/screens         │
      │  lib/widgets         │
      │  lib/models/services │
      └─────┬────────────────┘
            │
┌───────────┴───────────────────────────────────────────────────────────────┐
│                         配信/ホスティング（静的）                          │
│  ┌──────────────────────┐   ┌──────────────────────────────────────────┐  │
│  │ GitHub Pages         │←──│ docs/（mainブランチ）または gh-pages ブランチ│
│  └──────────────────────┘   └──────────────────────────────────────────┘  │
│  ┌────────────────────────────┐                                           │
│  │ Service Worker（PWA/Cache）│  build 産物: index.html, main.dart.js 他   │
│  └────────────────────────────┘                                           │
└────────────────────────────────────────────────────────────────────────────┘
            │
            ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                           バックエンド（データ）                           │
│  ┌───────────┐   ┌──────────────────────┐                                  │
│  │ Firestore │←→ │ lib/services/*       │  firestore.rules / indexes      │
│  └───────────┘   └──────────────────────┘                                  │
│  ┌──────────────────────────┐                                               │
│  │ Data Connect（拡張/任意）│  /dataconnect/*                               │
│  └──────────────────────────┘                                               │
└───────────────────────────────────────────────────────────────────────────┘


                         ┌─────────────────────┐
                         │    運用 / ツール     │
                         ├─────────────────────┤
                         │ build.ps1           │
                         │ docs/*（Pages用） │
                         │ vote_options_editor │
                         │ convert_json_to_xlsx│
                         │ docs/guide (VitePress)
                         └─────────────────────┘
```
